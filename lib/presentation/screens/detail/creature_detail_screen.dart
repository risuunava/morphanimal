import 'package:flutter/material.dart';

class CreatureDetailScreen extends StatelessWidget {
  const CreatureDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('CreatureDetailScreen')),
      body: const Center(child: Text('CreatureDetailScreen Placeholder')),
    );
  }
}
